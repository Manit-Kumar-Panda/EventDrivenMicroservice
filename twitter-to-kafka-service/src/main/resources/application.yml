spring:
  application:
    name: twitter-to-kafka-service
  profiles:
    active: twitter_to_kafka
  config:
    import: "configserver:"
  cloud:
    config:
      name: config-client
      uri: http://config-server:8888
      username: username
      password: password



jasypt:
  encryptor:
    password: ${S}
